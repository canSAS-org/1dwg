<?xml version="1.0" encoding="UTF-8"?>
<?oxygen RNGSchema="http://www.oasis-open.org/docbook/xml/5.0/rng/docbook.rng" type="xml"?>
<!-- 
    ########### SVN repository information ###################
    # $Date$
    # $Author$
    # $Revision$
    # $HeadURL$
    # $Id$
    ########### SVN repository information ###################
-->
<chapter xml:id="specification" xmlns="http://docbook.org/ns/docbook" version="5.0"
    xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude">
    <title>cansas1d/1.0 Specification</title>
    <para>
        This is the definitive specification of 
        <literal>cansas1d/1.0</literal>,
        <indexterm><primary>cansas1d/1.0 standard</primary></indexterm> 
        the canSAS standard format for storing small-angle
        scattering data in XML files.
        The standard is defined using the rules of 
        <link xlink:href="http://www.w3.org/XML/Schema">XML Schema</link>.
    </para>
    <para>
        Note that the cansas1d/1.0 XML data files must adhere to the 
        XML rules which includes being well-formed
        <indexterm><primary>XML</primary><secondary>well-formed</secondary></indexterm>
        (including the use of closing tags).  
        <footnote>
            <para>
                For example, see 
                <link xlink:href="http://www.w3schools.com/xmL/xml_syntax.asp"
                    ><literal>http://www.w3schools.com/xmL/xml_syntax.asp</literal></link>
                for an explanation of the XML syntax.
            </para>
        </footnote>
        Files that can be validated 
        <indexterm><primary>cansas1d/1.0 standard</primary></indexterm>
        <indexterm><primary>validation</primary></indexterm>
        against 
        <link
            xlink:href="http://svn.smallangles.net/trac/canSAS/browser/1dwg/trunk/cansas1d.xsd"
            ><literal>cansas1d.xsd</literal> </link>
        are deemed to be valid cansas1d/1.0 data files.
    </para>
    <para>
        In this document, curly braces, <literal>{}</literal>, are used to
        indicate text that is supplied by the user.  Such as, an attribute
        may be written
        <informalexample>
            <programlisting>name={text}</programlisting>
        </informalexample>
        and this means that the 
        user would replace <literal>{text}</literal> with text that gives,
        in this example, a name such as <literal>final detector</literal>.
        Thus resulting in
        <informalexample>
            <programlisting>name="final detector"</programlisting> 
        </informalexample>
        which is a well-formed XML attribute.  
    </para>
    <para>
        Another example is an instance of the 
        <link xlink:href="#cansas1d_documentation-element-any"><literal>{any}</literal></link>
        element.  Suppose one had analysis data, then <literal>{any}</literal> would be replaced 
        with <literal>analysis</literal> and the element might look like this:
        <informalexample>
            <programlisting>
                &lt;analysis>
                ... analysis content goes here ...
                &lt;/analysis>
            </programlisting>
        </informalexample>
    </para>


    <xi:include href="elements.xml"/>
</chapter>
